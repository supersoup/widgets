<thead>
<tr>
    <% for (var i = 0; i < colList.length; i ++) { %>
    <th class="<%= colList[i].sorter ? 'wd-tree-table-head-sorter' : '' %>"
        data-index="<%= i %>"
    >
        <span class="wd-tree-table-head-text"><%- colList[i].title %></span>
    </th>
    <% } %>
</tr>
</thead>